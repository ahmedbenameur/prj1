pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                sh './mvnw clean verify sonar:sonar -Dsonar.host.url=http://so-sonarqube-1:9000'
            }
        }
    }
}
